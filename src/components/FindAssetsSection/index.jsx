import React, { Component } from 'react';
import ContentCenter        from '../ContentCenter';

export default class FindingAssetsSection extends Component {
    render() {
        return (
            <ContentCenter>
                <span>Finding images...</span>
            </ContentCenter>
        );
    }
}